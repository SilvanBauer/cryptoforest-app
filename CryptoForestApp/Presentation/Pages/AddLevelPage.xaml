<Page
    x:Class="CryptoForestApp.Presentation.Pages.AddLevelPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CryptoForestApp.Presentation.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Grid Padding="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <ComboBox x:Uid="ParentLevelComboBox" Margin="5" ItemsSource="{Binding Levels}" SelectedItem="{Binding SelectedItem}" SelectedIndex="{Binding SelectedLevelIndex, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
            <ComboBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Text}" />
                </DataTemplate>
            </ComboBox.ItemTemplate>
        </ComboBox>

        <TextBox x:Uid="LevelNameTextBox" Grid.Row="1" Margin="5" Text="{Binding LevelName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

        <Button x:Uid="AddButton" Grid.Row="2" Margin="5" VerticalAlignment="Bottom" Command="{Binding AddAsync}" />
        <Button x:Uid="BackButton" Grid.Row="2" Margin="5" HorizontalAlignment="Right" VerticalAlignment="Bottom" Command="{Binding BackAsync}" />
    </Grid>
</Page>
