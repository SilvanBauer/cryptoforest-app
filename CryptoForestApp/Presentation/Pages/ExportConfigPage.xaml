<Page
    x:Class="CryptoForestApp.Presentation.Pages.ExportConfigPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CryptoForestApp.Presentation.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Grid Padding="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>

        <TreeView Margin="5" ItemsSource="{Binding Levels}" SelectionChanged="LevelSelectionChanged" CanDragItems="False" AllowDrop="False" SelectionMode="Multiple">
            <TreeView.ItemTemplate>
                <DataTemplate>
                    <TreeViewItem ItemsSource="{Binding Sublevels}" IsExpanded="true">
                        <TextBlock Text="{Binding LevelKey}" />
                    </TreeViewItem>
                </DataTemplate>
            </TreeView.ItemTemplate>
        </TreeView>

        <PasswordBox x:Uid="PasswordTextBox" Grid.Row="1" Margin="5" IsPasswordRevealButtonEnabled="True" Password="{Binding Password, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

        <Button x:Uid="ExportButton" Grid.Row="2" Margin="5" Width="150" Command="{Binding ExportAsync}" />
        <Button x:Uid="BackButton" Grid.Row="2" Margin="5" Width="150" HorizontalAlignment="Right" Command="{Binding BackAsync}" />
    </Grid>
</Page>
