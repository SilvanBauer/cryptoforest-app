<Page x:Class="CryptoForestApp.Presentation.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:CryptoForestApp"
      Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Grid Padding="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="auto" />
        </Grid.ColumnDefinitions>

        <TextBlock x:Uid="HistoryTextBlock" Grid.ColumnSpan="2" Margin="5" FontWeight="Bold" />

        <ListView Grid.Row="1" Grid.ColumnSpan="2" Margin="5" ItemsSource="{Binding History}" SelectedItem="{Binding SelectedHistoryItem, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding}" />
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="5" Spacing="5">
            <Button x:Uid="OpenButton" Width="150" Command="{Binding OpenAsync}" />
            <Button x:Uid="AddButton" Width="150"  />
            <Button x:Uid="CreateButton" Width="150"  />
        </StackPanel>
        <Button x:Uid="CancelButton" Grid.Row="2" Grid.Column="1" Margin="5" Width="150" Click="Cancel" />
    </Grid>
</Page>
