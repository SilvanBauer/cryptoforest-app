<ContentDialog
    x:Uid="DecryptedDialog"
    x:Class="CryptoForestApp.Presentation.Dialogs.DecryptedContentDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CryptoForestApp.Presentation.Dialogs"
    xmlns:converters="using:CryptoForestApp.Converters"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Style="{ThemeResource DefaultContentDialogStyle}">
    <ContentDialog.Resources>
        <converters:VisibilityConverter x:Key="VisibilityConverter" />
    </ContentDialog.Resources>
	<Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>

        <TextBlock x:Uid="DecryptedTextBlock" />

        <TextBox Grid.Row="1" Margin="0 5 0 0" Text="{Binding Text}" Visibility="{Binding HasText, Converter={StaticResource VisibilityConverter}}" />
    </Grid>
</ContentDialog>
