<ContentDialog
    x:Uid="MoveDialog"
    x:Class="CryptoForestApp.Presentation.Dialogs.MoveContentDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CryptoForestApp.Presentation.Dialogs"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Style="{ThemeResource DefaultContentDialogStyle}"
    PrimaryButtonCommand="{Binding MoveAsync}">
    <Grid>
        <ComboBox x:Uid="NewLevelComboBox" ItemsSource="{Binding Levels}" SelectedIndex="{Binding SelectedLevelIndex, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
            <ComboBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Text}" />
                </DataTemplate>
            </ComboBox.ItemTemplate>
        </ComboBox>
    </Grid>
</ContentDialog>
