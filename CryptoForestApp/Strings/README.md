# Strings
Contains the translations of the application. Each text has it's own key and the whole application is translatable as every single text was added as a string here.
Currently supported languages are de and en. The application will automatically choose the language based on the localization.
